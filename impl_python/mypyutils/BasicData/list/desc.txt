# -*- coding:utf-8 -*-

#列表和索引：（1）正向索引和逆向索引（2）列表可以是异构的，即列表中的类型可以是不同类型
#切片：可以对列表进行切片操作，即mylist[1:4]
#别名机制：（1）两个变量引用同一个值时，他们互为别名（2）函数调用中入参即为别名
#列表方法：只有pop是返回值，其他方法都是在原有列表上进行操作
#嵌套列表：将子列表赋值给一个变量后，相当于给子列表起了一个别名
#元组：元组在创建以后，其所包含的引用就不能再修改了，而这些引用所指向的对象本身仍然是可以修改的
#把文件看做列表：（1）read()：如果没有提供参数，则读取文件中所有数据，并以字符串形式返回。
#                   没有其他数据时，会返回一个空字符串。在for循环中，read方法只是读取一行数据
#                 （2）readline()
#               (3)strip()：去掉行结束符、空白符等
#               (4)命令行参数详见commandArgs.py


