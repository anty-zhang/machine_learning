# -*- coding:utf-8 -*-

###set
##集合运算：详见setTest.py
##集合存储：存储集合的数据结构为hash table。
#           当某个元素放入散列表后，其散列码就不能再改变了。
#           当某列表的内容发生变化时，其散列码也会相应发生变化，原因是：散列码是通过对值的计算而得来。因此
#                     python只允许集合含有不变值(immutable)，布尔值、数字、字符串、元组可以，而列表不可以

#不能创建集合的集合：在集合中可能加入或删除元素，所以不能再集合中存放另一个集合。
#                    解决办法：frozen set
#frozenset(values)：创建带有值的冻集合。其中values可以是列表、元组、集合


###dict


